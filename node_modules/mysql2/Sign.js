const loginForm = document.getElementById("login-form");

import { createConnection } from 'C:/Users/Виктория/Desktop/Моя_папка/1 хокотон/FriQuest/node_modules/mysql2/index.js';

const connection = createConnection({
  host: "localhost",
  user: "root",
  database: "friquest",
  password: "14708642"
});

connection.connect(function(err){
    if (err) {
      return console.error("Ошибка: " + err.message);
    }
    else{
      console.log("Подключение к серверу MySQL успешно установлено");
    }
 });
loginForm.addEventListener("submit", function(event) {
    event.preventDefault();

    const usernameInput = document.getElementById("username");
    const usernamePInput = document.getElementById("usernameP");
    const passwordInput = document.getElementById("password");

    const username = usernameInput.value;
    const usernameP = usernamePInput.value;
    const password = passwordInput.value;

});

